[build]
  publish = "build/web"
  # Build already done locally/in CI - just deploy the files
  command = "echo 'Using pre-built files from build/web'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
