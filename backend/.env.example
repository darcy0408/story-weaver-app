# Environment variables for Story Weaver Backend
# Copy this file to .env and fill in your actual values

# ==========================================
# REQUIRED ENVIRONMENT VARIABLES
# ==========================================

# Google Gemini API Key (required for story generation)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# OpenRouter API Key (required for image generation)
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# ==========================================
# OPTIONAL ENVIRONMENT VARIABLES
# ==========================================

# Database Configuration
# For local development (SQLite) - leave empty for default
# DATABASE_URL=
# For production (PostgreSQL on Railway)
# DATABASE_URL=postgresql://username:password@host:port/database

# Gemini Model Configuration (optional)
# Available models: gemini-2.5-flash, gemini-1.5-flash, etc.
GEMINI_MODEL=gemini-2.5-flash

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=True

# Server Configuration
# Port for the Flask app (optional, defaults to 5000)
PORT=5000

# CORS Origins (for production deployments)
# Comma-separated list of allowed origins
# ALLOWED_ORIGINS=https://yourdomain.com,https://anotherdomain.com

# ==========================================
# DEVELOPMENT NOTES
# ==========================================
# 1. Never commit your actual .env file to version control
# 2. For production, set FLASK_ENV=production and FLASK_DEBUG=False
# 3. Database URL will default to SQLite if not provided
# 4. All API keys are required for full functionality